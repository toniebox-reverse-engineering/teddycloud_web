export const MAX_FILES = 99;
export const DEFAULT_UPLOAD_TIMEOUT_MS = 120000;

/** Debounce for search/filter inputs (file browser table filter, image manager original URLs). */
export const UI_SEARCH_DEBOUNCE_MS = 250;

/** `setTimeout` fallback when `requestIdleCallback` is missing — toniesJson prefetch in image modal. */
export const SELECT_IMAGE_JSON_PREFETCH_FALLBACK_MS = 400;
